dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
visualise_landscape(landscape)
visualise_landscape(landscape, density = H[,20])
dots <- dot_density_points(
landscape, density = H[,5], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1)
}
{
timesteps <- 10
max_host_dispersal <- 5 # number of adjacent cells
scale <- 1 # drop-off strength
stay_prob <- 0.5  # probability of staying in cell
beta <- 1 # habitat preference
survival <- 1 # dispersal survival rate
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 15) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
visualise_landscape(landscape)
visualise_landscape(landscape, density = H[,timesteps])
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1)
}
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, seed = 1)
visualise_landscape(landscape, density = density, dots = dots)
})
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 5)
visualise_landscape(landscape, density = density, dots = dots)
})
# combine all plots in a grid
wrap_plots(plots, ncol = 4)  # adjust ncol for layout
source("~/R/simulating-ftc-movement/code/mapping.R")
visualise_landscape(landscape, dots = dots, dotsize = 1)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, density = density, dots = dots)
})
# combine all plots in a grid
wrap_plots(plots, ncol = 4)  # adjust ncol for layout
visualise_landscape(landscape, density = density, dots = dots, show_legend = FALSE)
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, density = density, dots = dots, show_legend = FALSE)
})
# combine all plots in a grid
wrap_plots(plots, ncol = 4)  # adjust ncol for layout
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
})
# combine all plots in a grid
wrap_plots(plots, ncol = 4)  # adjust ncol for layout
source("~/R/simulating-ftc-movement/code/mapping.R")
source("~/R/simulating-ftc-movement/code/mapping.R")
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
source("~/R/simulating-ftc-movement/code/mapping.R")
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
})
# combine all plots in a grid
wrap_plots(plots, ncol = 4)  # adjust ncol for layout
{
timesteps <- 9
max_host_dispersal <- 5 # number of adjacent cells
scale <- 1 # drop-off strength
stay_prob <- 0.5  # probability of staying in cell
beta <- 1 # habitat preference
survival <- 1 # dispersal survival rate
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 15) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
visualise_landscape(landscape)
visualise_landscape(landscape, density = H[,timesteps])
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
}
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
})
# combine all plots in a grid
wrap_plots(plots, ncol = 3)  # adjust ncol for layout
source("~/R/simulating-ftc-movement/code/mapping.R")
{
timesteps <- 9
max_host_dispersal <- 5 # number of adjacent cells
scale <- 1 # drop-off strength
stay_prob <- 0.5  # probability of staying in cell
beta <- 1 # habitat preference
survival <- 1 # dispersal survival rate
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 15) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
visualise_landscape(landscape)
visualise_landscape(landscape, density = H[,timesteps])
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
}
source("~/R/simulating-ftc-movement/code/mapping.R")
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
# Time-evolved with dot-density
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
wrap_plots(plots, ncol = 3)  # adjust ncol for layout
})
# Time-evolved with dot-density
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
wrap_plots(plots, ncol = 3)  # adjust ncol for layout
}
wrap_plots(plots, ncol = 3)  # adjust ncol for layout
# Time-evolved with dot-density
{
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
})
wrap_plots(plots, ncol = 3)  # adjust ncol for layout
}
{
timesteps <- 9
max_host_dispersal <- 8 # number of adjacent cells
scale <- 1 # drop-off strength
stay_prob <- 0.5  # probability of staying in cell
beta <- 1 # habitat preference
survival <- 1 # dispersal survival rate
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 15) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
}
# Time-evolved with dot-density
{
plots <- lapply(1:timesteps, function(t) {
density <- H[, t]
dots <- dot_density_points(landscape, density, dot_clutter = 3)
visualise_landscape(landscape, dots = dots, show_legend = FALSE)
})
wrap_plots(plots, ncol = 3)  # adjust ncol for layout
}
visualise_landscape(landscape)
visualise_landscape(landscape, density = H[,timesteps])
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
{
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
}
visualise_landscape(landscape, density = H[,6])
dots <- dot_density_points(
landscape, density = H[,timesteps], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
dots <- dot_density_points(
landscape, density = H[,6], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
dots <- dot_density_points(
landscape, density = H[,4], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
source("code/mapping.R")
source("code/model.R")
{
timesteps <- 9
max_host_dispersal <- 8 # number of adjacent cells
scale <- 1 # drop-off strength
stay_prob <- 0.5  # probability of staying in cell
beta <- 1 # habitat preference
survival <- 1 # dispersal survival rate
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 15) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
}
visualise_landscape(landscape)
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 10) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
{
timesteps <- 9
max_host_dispersal <- 8 # number of adjacent cells
scale <- 1 # drop-off strength
stay_prob <- 0.5  # probability of staying in cell
beta <- 1 # habitat preference
survival <- 1 # dispersal survival rate
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 10) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
}
visualise_landscape(landscape)
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 20) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
{
timesteps <- 9
max_host_dispersal <- 8 # number of adjacent cells
scale <- 1 # drop-off strength
stay_prob <- 0.5  # probability of staying in cell
beta <- 1 # habitat preference
survival <- 1 # dispersal survival rate
# load landscape
landscape <- load_landscape("images/nb-scape-bw-thick.png", scale = 0.5, # Set scale <1 to shrink image
cellsize = 20) # set cellsize <8 to make a finer hex grid overlay
landscape <- basic_habitat_quality(landscape, threshold = 0.5) # set habitat quality threshold
landscape$Q <- ifelse(landscape$type == "high", 1, 0)
# Compute neighbours
neighbs <- compute_neighbors(landscape, max_host_dispersal)
# Compute neighbour distances
dists <- compute_sparse_distance(landscape, neighbs, max_host_dispersal)
# Generate dispersal kernel
K <- initialize_dispersal(
dists, # sparse distance matrix
scale, # drop-off scale
stay_prob, # probability of staying in cell
kernel_function = "negative_exp",
beta, # habitat preference strength
Q = landscape$Q # habitat quality vector
)
# Initialize density
H <- matrix(0, nrow = nrow(landscape), ncol = timesteps)
H[landscape$type == "high",1] = 1
H[,1] <- H[,1] / sum(H[,1])
# Disperse
for (i in 2:timesteps) {
H[,i] <- disperse(
kernel = K,
density = H[,i-1],
survival
)
}
}
visualise_landscape(landscape)
dots <- dot_density_points(
landscape, density = H[,1], dot_clutter = 5)
visualise_lansdscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
{
dots <- dot_density_points(
landscape, density = H[,1], dot_clutter = 5)
visualise_lansdscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
}
source("~/R/simulating-ftc-movement/code/mapping.R")
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
max_host_dispersal <- 5 # number of adjacent cells
dots <- dot_density_points(
landscape, density = H[,2], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
dots <- dot_density_points(
landscape, density = H[,3], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
dots <- dot_density_points(
landscape, density = H[,4], dot_clutter = 5)
visualise_landscape(landscape, dots = dots, dotsize = 1, show_legend = FALSE)
